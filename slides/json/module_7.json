[
  {
    "type": "cover",
    "script": {
      "name": "drone",
      "opts": {
        "perched": true,
        "anchor": ".cover-card",
        "anchorOffsetX": 100,
        "anchorOffsetY": -15,
        "scale": 1.05,
        "color": "#22c55e"
      }
    },
    "bg": 2,
    "h1": "Module 7",
    "lead": "RESTful APIs: OpenAPI",
    "sub": "Technology block (a): describing every interface in the UTM diagram",
    "badges": [
      "OpenAPI 3.1",
      "Swagger UI",
      "Spec-First",
      "Code Generation",
      "*Live Demo"
    ]
  },
  {
    "type": "content",
    "mod": "7",
    "tag": "",
    "title": "What Is OpenAPI?",
    "desc": "A specification for describing RESTful APIs — machine-readable, human-friendly",
    "body": [
      {
        "card": "The basics",
        "items": [
          "OpenAPI is a <strong>specification for describing RESTful APIs</strong>",
          "Enables a standardized definition of API endpoints and their functions",
          "Originally developed as <strong>Swagger</strong>, now governed under the Linux Foundation (OpenAPI Initiative)"
        ]
      },
      {
        "card": "Why use OpenAPI?",
        "items": [
          "<strong>Standardization</strong> — uniform API documentation across teams and vendors",
          "<strong>Automatic generation</strong> — code, documentation and test cases generated from the spec",
          "<strong>Interactive use</strong> — try APIs live, e.g. in Swagger UI",
          "<strong>Easier API design</strong> — collaboration between developers and stakeholders on one artifact",
          "A NOTAM service, a METAR service (Module 12), a UTM clearance service — all of them need exactly this kind of contract"
        ]
      }
    ]
  },
  {
    "type": "content",
    "mod": "7",
    "tag": "",
    "title": "Anatomy of an OpenAPI Document",
    "desc": "APIs are described in YAML or JSON — five top-level building blocks",
    "body": [
      {
        "code": "<span class=\"kw\">openapi:</span> 3.1.0\n<span class=\"kw\">info:</span>\n  title: UTM Flight Clearance API\n  version: 1.0.0\n<span class=\"kw\">servers:</span>\n  - url: https://utm.example.com/v1\n<span class=\"kw\">paths:</span>\n  /drones/{droneId}/clearance:\n    post:\n      summary: Request flight clearance for a mission\n      responses:\n        <span class=\"str\">'201'</span>: { description: Clearance granted }\n        <span class=\"str\">'409'</span>: { description: Conflict with another mission }"
      },
      {
        "card": "The five building blocks",
        "items": [
          "<code>openapi</code> — version of the specification, <code>info</code> — metadata: title, version, description",
          "<code>servers</code> — available servers, <code>paths</code> — definition of the API endpoints",
          "<code>components</code> — reusable schema definitions (e.g. a <code>Mission</code> object shared by several endpoints)",
          "Use <strong>3.1</strong> for new work: its schemas are plain <strong>JSON Schema</strong>, and every generator supports it today"
        ]
      }
    ]
  },
  {
    "type": "content",
    "mod": "7",
    "tag": "important",
    "title": "The Course Contract: telemetry.yaml",
    "desc": "The one interface every project touches — the Android client posts a fix, the backend accepts it",
    "body": [
      {
        "code": "<span class=\"kw\">openapi:</span> 3.1.0\n<span class=\"kw\">info:</span> { title: Drone Telemetry API, version: 1.0.0 }\n<span class=\"kw\">paths:</span>\n  /drones/{droneId}/fixes:\n    post:\n      summary: Ingest one position fix from the Android client (M5)\n      parameters:\n        - { name: droneId, in: path, required: true, schema: { type: string } }\n      requestBody:\n        required: true\n        content:\n          application/json:\n            schema: { $ref: <span class=\"str\">'#/components/schemas/Fix'</span> }\n      responses:\n        <span class=\"str\">'202'</span>: { description: Accepted &mdash; published to telemetry.topic (M9) }\n        <span class=\"str\">'422'</span>: { description: Rejected &mdash; altitude above 120 m or timestamp in the future }\n<span class=\"kw\">components:</span>\n  schemas:\n    Fix:\n      type: object\n      required: [lat, lon, altMeters, ts]\n      properties:\n        lat:       { type: number, minimum: -90,  maximum: 90 }\n        lon:       { type: number, minimum: -180, maximum: 180 }\n        altMeters: { type: number, minimum: 0,    maximum: 120 }\n        ts:        { type: string, format: date-time }"
      },
      {
        "card": "Read the contract",
        "items": [
          "<code>Fix</code> is the <strong>same object</strong> Module 9 publishes on the topic and Module 14 draws on the map &mdash; one schema, three modules",
          "The unit is in the name (<code>altMeters</code>) &mdash; the Module 2 rule, applied at the API boundary",
          "<code>422</code> turns the open-category ceiling into a <strong>contract rule</strong>: the server refuses what the law forbids &mdash; and Module 11 tests exactly that response"
        ]
      }
    ]
  },
  {
    "type": "content",
    "mod": "7",
    "tag": "demo",
    "title": "From Spec to Code: Generate Both Ends",
    "desc": "Spec-first — the YAML is the source of truth, client and server are derived from it",
    "body": [
      {
        "code": "<span class=\"kw\"># Kotlin client for the Android app (M5)</span>\nopenapi-generator-cli generate -i telemetry.yaml -g kotlin -o client/\n\n<span class=\"kw\"># Spring WebFlux server stub (M8)</span>\nopenapi-generator-cli generate -i telemetry.yaml -g spring \\\n    --additional-properties=reactive=true -o server/\n\n<span class=\"kw\"># Swagger UI served by the running service (build.gradle.kts)</span>\nimplementation(<span class=\"str\">\"org.springdoc:springdoc-openapi-starter-webflux-ui:2.8.0\"</span>)"
      },
      {
        "card": "What you get — and what you still write",
        "items": [
          "<strong>Generated</strong>: a typed client (<code>TelemetryApi.postFix(droneId, fix)</code>), the server interface with routes and DTOs, and the <code>Fix</code> class on both sides &mdash; the same object the app sends and the server validates",
          "<strong>Yours</strong>: the handler body (publish to the topic), and any rule the schema cannot express &mdash; e.g. <em>this drone has no clearance for this zone</em>"
        ]
      },
      {
        "card": "Two ways to get a spec",
        "items": [
          "<strong>Spec-first</strong>: write the YAML, generate the code &mdash; this course, because the Android client (M5) and the Karate tests (M11) are built from the <strong>same file</strong>",
          "<strong>Code-first</strong>: annotate Spring controllers, let springdoc emit the YAML &mdash; quick for one team, drifts as soon as a second consumer appears",
          "Either way: <strong>never edit generated code</strong> &mdash; change the spec, regenerate"
        ],
        "warn": true
      }
    ]
  },
  {
    "type": "content",
    "mod": "7",
    "tag": "demo",
    "title": "OpenAPI Tooling: Swagger Editor & Friends",
    "desc": "One spec, a whole toolchain — live demo in the editor",
    "body": [
      {
        "card": "The toolbox",
        "items": [
          "<a href=\"https://swagger.io/tools/swagger-ui/\" target=\"_blank\"><strong>Swagger UI</strong></a> — visual rendering and in-browser test calls (try the <a href=\"https://editor.swagger.io\" target=\"_blank\">live editor</a>)",
          "<strong>Redoc</strong> — modern, read-oriented API documentation",
          "<strong>Postman</strong> — API testing and development",
          "<strong>OpenAPI Generator</strong> — client/server code generation for dozens of languages"
        ]
      },
      {
        "img": "swagger-editor-petstore.jpg",
        "alt": "Swagger Editor with the Petstore example: YAML on the left, rendered documentation with GET/POST endpoints on the right",
        "height": 280,
        "caption": "editor.swagger.io — YAML on the left, rendered documentation on the right, 'Try it out' sends real requests."
      },
      {
        "card": "Live demo — follow along on your laptop",
        "items": [
          "<strong>1.</strong> Open <a href=\"https://editor.swagger.io\" target=\"_blank\">editor.swagger.io</a>, delete the Petstore, paste <code>telemetry.yaml</code> from two slides ago &mdash; the docs render as you type",
          "<strong>2.</strong> Change <code>maximum: 120</code> to <code>maximum: 12</code> and watch the rendered schema follow; break the YAML indentation and watch the validator catch it",
          "<strong>3.</strong> Add a <code>servers: [{ url: http://localhost:8080 }]</code> entry, start the generated stub from the next slide, click <strong>Try it out</strong> on <code>POST /fixes</code> with <code>altMeters: 150</code> &mdash; expect <strong>422</strong>",
          "<strong>4.</strong> Generate &rarr; Client &rarr; <em>kotlin</em>: the zip you download is the Android client's API layer"
        ]
      }
    ]
  },
  {
    "type": "content",
    "mod": "7",
    "tag": "exercise",
    "title": "Rename a Field, Break a Client",
    "desc": "Why spec-first — and what 'breaking change' means when one of your consumers is an app in someone's pocket",
    "body": [
      {
        "code": "<span class=\"kw\"># telemetry.yaml — someone \"cleans up\" a name</span>\n-        altMeters: { type: number, minimum: 0, maximum: 120 }\n+        altitude:  { type: number, minimum: 0, maximum: 120 }\n\n<span class=\"kw\">// Android app, after regenerating the client (M5)</span>\nval fix = Fix(lat = 48.0877, lon = 9.2160, altMeters = 38.5, ts = now())\n                                            <span class=\"str\">^^^^^^^^^ error: no parameter named 'altMeters'</span>\n\n<span class=\"kw\">// the same app with a hand-written JSON client instead</span>\n{ \"lat\": 48.0877, \"lon\": 9.2160, \"altMeters\": 38.5, \"ts\": \"…\" }   <span class=\"str\">// compiles, ships, and</span>\n                                                                     <span class=\"str\">// the server sees altitude = null</span>"
      },
      {
        "card": "What just happened",
        "items": [
          "With a <strong>generated client</strong>, the compiler is the first contract test: the rename fails the build at the desk, not the flight",
          "With a <strong>hand-written client</strong> the field silently disappears &mdash; and whether the server answers 422 or accepts a fix without altitude is now a matter of luck",
          "Rule: <strong>additive changes</strong> (new optional field, new endpoint) are free; <strong>renames and removals</strong> are a new major version &mdash; <code>/v2</code> in <code>servers</code>, old clients keep working on <code>/v1</code>"
        ]
      },
      {
        "card": "Try it — five minutes",
        "items": [
          "Rename the field in the editor, regenerate the Kotlin client, find the compile error in the lab app. Then add an optional <code>headingDeg</code> instead and confirm nothing breaks"
        ]
      }
    ]
  },
  {
    "type": "content",
    "mod": "7",
    "tag": "takeaway",
    "title": "Module 7: Key Takeaways",
    "body": [
      {
        "card": "",
        "items": [
          "OpenAPI describes a REST API as a <strong>single machine-readable contract</strong> — humans and tooling read the same artifact",
          "From that one spec you generate <strong>documentation, client and server code</strong> — and Module 11 derives the tests from it",
          "<strong>Spec-first</strong>: the YAML is the source of truth; generated code is never edited, only regenerated",
          "<code>telemetry.yaml</code> is the course's contract: the <code>Fix</code> schema travels unchanged from the Android app (5) through the topic (9) to the map (14)"
        ]
      }
    ]
  }
]
